{
  "name": "@qqtools3/main",
  "version": "5.7.0",
  "scripts": {
    "start": "npm run clean && tsc --outDir lib --watch",
    "dev": "npm run clean && tsc --outDir lib",
    "build": "npm run clean && node webpackBuild.mjs",
    "clean": "rimraf ./lib && rimraf ./.lib.mid"
  },
  "dependencies": {
    "chrome-launcher": "^0.15.2",
    "chrome-remote-interface": "^0.33.0",
    "node-nim": "^9.16.5",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
